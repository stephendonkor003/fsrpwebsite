---
paths:
  - '{app/Http/Controllers/SiteController.php,resources/views/site/**,database/seeders/SeedInvestmentSummitSeeder.php}'
---

# Seeders

## Archive previous events; feature only the current event
The public events archive defaults to all published events, including past events. The homepage hero, event spotlight, programme, and resources stay scoped to the configured current Seed Summit. Preserve historical event and slide records; deactivate obsolete homepage slides instead of deleting them.
