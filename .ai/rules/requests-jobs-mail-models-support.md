---
paths:
  - 'app/{Actions,Casts,Http/Controllers,Http/Requests,Jobs,Mail,Models,Support}/**'
---

# Requests Jobs Mail Models Support

## Keep delegate registrations private
Encrypt identity, contact, travel fields, and uploaded documents. Keep full receipts and PDFs behind the originating session plus short-lived signed URLs. Email only a scanner-safe signed GET landing that requires an explicit signed POST to verify. Preserve the consent wording hash, and prune stale records through model:prune so private files are deleted with their registration.
